# vo=dmabuf-wayland      # Optimized for Hyprland/Wayland
vo=gpu
hwdec=auto-copy        # Safer than vaapi for unsupported codecs
profile=fast           # Skip unnecessary processing
gpu-api=vulkan         # Modern GPU API (if supported)

# save-position-on-quit

